# Steps

Maintainer notes that live beside the steps: one Markdown file per step, named
`step-<order>-<id>.md`, with the step id in the frontmatter matching the stem.

This file has no frontmatter and is not a step. `loadProject` ignores it.
