"""docsandeye_render — turn a render plan into rendered artefacts.

Stdlib-only (Python 3.11+).  The package is driven by the Node CLI as
``python3 -m docsandeye_render …``; it never reads YAML and never touches the
component sources beyond handing paths to a driver.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
