## task_003 — starlight-docsandeye — cycle 1 Generator — 2026-09-04T22:05Z
Generator (fable) committed dd31363 on vsss/task_003-plugin (after `git merge main` db089d0 for the fixture path-warn allowlist). Plugin: config:setup loads the project via core, resolves the theme pack (stock `starlight` in-package, else `@docsandeye/themes/<name>.css`, else `unknown theme "<name>"`), appends docsandeye.css + theme css, merges Head/Sidebar/PageTitle overrides (user keys win), adds an Astro integration that injects `<base>` + `<base>/[step]` per guide (getStaticPaths keyed on `routePattern`) and `/reshoot` in maintainer builds, serves `virtual:docsandeye/model`, and copies renders + local media into `dist/_docsandeye/` at astro:build:done. `astro build` of fixtures/site: 7 pages (8 with DOCSANDEYE_MAINTAINER=1), exit 0; site-bad-theme exits 1 with `unknown theme "nope"` on stderr. Registering script 3250 B (dynamic-import chunk model-viewer.min 1.02 MB). 18/18 scratch tests green; root `npm test` 213/213; `npm run build` and typecheck clean. Report: .vs/cycle-1/generator-report.md.
